import React from "react";
import { CContainer } from "@coreui/react";
// routes config
const BookContent = () => {
    return <CContainer lg>Books</CContainer>;
};

export default React.memo(BookContent);
